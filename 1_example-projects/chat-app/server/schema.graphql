type Query {
  messages: [Message]
}

type Mutation {
  addMessage(input: MessageInput!): Message
}

# Use a name that describes an event
type Subscription {
  messageAdded: Message
}

type Message {
  id: ID!
  from: String
  text: String
}

input MessageInput {
  text: String
}
